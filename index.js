$("button").click( () => console.log("button pressed"));

function buttonResponse() {
    // var buttonColor = $("button").css("background-color");
    
    // $("button").css("background-color", "grey");

    // setTimeout(200, $("button").css("background-color", buttonColor));
}